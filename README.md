# hoshi-mi-cf

A GrowthForecast-like metrics service that runs entirely within the Cloudflare free tier.
Push numbers with `curl`, get time series graphs rendered with Chart.js. No RRDtool,
no server to maintain.

## Status

Work in progress.
